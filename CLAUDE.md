# Attendance Management System — Project Notes

## Structure (feature-module architecture)
- Entry: `src/main.jsx` (wraps `<App/>` in `QueryClientProvider` + `ErrorBoundary`) → `src/app/App.jsx` (root shell: sidebar/topnav/bottom-nav/module routing, dark mode toggle). This is the LIVE app.
- Each module lives in its own folder under `src/features/<name>/`, typically:
  - `<Name>Page.jsx` — the UI component
  - `<name>.mock.js` — seed/generated mock data (also holds `CURRENT_USER`, `MY_REPORTEES`, `getLeaveBalance`, `getReportingChain` where relevant)
  - `<name>.service.js` — async CRUD layer, already shaped like a future real API call (`async function getX() { await delay(); return MOCK_DATA; }`) — this is the ONE place per feature that will need to change when a real backend exists
  - `hooks/use<Feature>Query.js` — **the data layer** (see TanStack Query section below) — replaces the old per-feature zustand `<name>.store.js` files, which have all been deleted
  - `<name>.validators.js` — form validation (present for every feature with a form — see Validation section)
  - `hooks/` — other feature-specific hooks (e.g. `features/employees/hooks/useEmployees.js` exports `useCurrentUser()`, `useMyReportees()`, `getReportingChain`)
  - `<name>.css` — **currently EMPTY on purpose** (see Styling below) — a few features (`attendance`, `leave`, `tax-declaration`) have a `@reference "../../styles/tailwind.css";` header comment + a handful of real `@apply` rules for genuinely repeated Tailwind combos; most stay fully empty
- Shared UI: `src/shared/components/` (`Btn`, `Modal`+`FormField`+`inputCls`, `DataTable`, `StatusBadge`, `CountUp`, `SearchableSelect`), `src/shared/mock/constants.js` (`DEPTS`, `LOCS`, etc.), `src/shared/utils/` (`cn`, `downloadTextFile`).
- Layout: `src/app/layout/Sidebar.jsx` (desktop, supports collapsible dropdown groups), `src/app/layout/TopNav.jsx` (breadcrumb + working global search — no message icon; just theme toggle, notifications bell, user menu), `src/app/layout/BottomNav.jsx` (**mobile-only**, `md:hidden`, fixed bottom tab bar: Home/Attendance/Leave/Documents + "More" opens the Sidebar drawer — native-app style), nav list in `src/app/nav.js`. The `dashboard` module is labeled **"Home"** in nav/breadcrumb (id stays `dashboard`); the `servicedesk` module is labeled **"HR-SUPPORT"** (id stays `servicedesk`) — don't rename the module ids, only the `label` in `nav.js`/heading text, since ids are threaded through routing, global search, and RBAC gating.
- No auth — `CURRENT_USER` (from employees mock) is the hardcoded "logged in" person. `MY_REPORTEES` = employees whose `manager` field equals `CURRENT_USER.name`. Role-based access is *simulated* on top of this single identity — see RBAC section.

### Dead code (removed)
The app was fully converted to plain JS/JSX. The old single-file `src/app/App.tsx`, the unused `src/app/components/ui/**` shadcn/ui scaffold (~47 files), the `figma/ImageWithFallback.jsx` helper, and several orphaned wrapper hooks/`globals.css` left over from refactors have all been confirmed unreferenced and deleted. Nothing in `src/app/` should exist outside `App.jsx`, `layout/`, and `nav.js`.

## Data layer — TanStack Query + zustand hybrid (current architecture)
- **TanStack Query owns server-state** (the entity lists), **zustand/local `useState` owns UI-only state** (filters, tabs, selected role, search text). Every feature's old `<name>.store.js` (zustand `createEntityStore`) has been deleted and replaced by `hooks/use<Feature>Query.js`, following the reference pattern in `src/features/employees/hooks/useEmployeesQuery.js`:
  - `use<Feature>Query()` — `useQuery({ queryKey, queryFn: get<Feature>, initialData: MOCK_ARRAY })` (initialData avoids a loading flash since this is still mock data).
  - `use<Feature>Mutations()` — one `useMutation` per CRUD op, each using `queryClient.setQueryData` for an **optimistic update**, an `onError` rollback (restores the pre-mutation cache snapshot), and `onSettled: () => queryClient.invalidateQueries(...)`.
- `src/shared/api/queryClient.js` is the ONE shared `QueryClient` (imported by `main.jsx` for the `QueryClientProvider`). `ATTENDANCE` is fully read-only now — there is no cross-feature mutation writing into its cache (the old WFH-approval-syncs-attendance side effect was removed along with the Work From Home leave tab).
- `src/shared/api/api.js` — a real axios instance (baseURL from `VITE_API_BASE_URL` or `/api`, request interceptor for a future auth token, response interceptor routing every HTTP error through the central error handler). **Not yet called by anything** — it's ready scaffolding for when a real backend exists; each feature's `.service.js` function body is the one-line change point (swap the mock-resolve body for `return api.get('/x').then(r => r.data)`).
- Global error handling (3 layers, all funnel into `src/shared/errors/errorHandler.js`'s `handleError()` — toast + console.error, so **no component should need its own try/catch for a data operation**):
  1. `api.js`'s axios response interceptor (HTTP/network errors).
  2. `queryClient.js`'s global `QueryCache`/`MutationCache` `onError`.
  3. `src/shared/errors/ErrorBoundary.jsx` wraps `<App/>` in `main.jsx` for render-time errors.
- Cross-cutting consumer files that read from MANY features' query hooks — always update these centrally, don't let a per-feature migration touch them piecemeal: `src/app/layout/TopNav.jsx` (global search reads employees/tickets/documents), `src/features/dashboard/DashboardPage.jsx` (reads leave/tickets/attendance). There is no Reports module anymore (removed — see Key modules).

## Role-based access (RBAC) — simulated, since there's no login
- `src/features/employees/employees.roles.js` — `getEmployeeRole(emp)` derives **Employee / Manager / HR Admin** from department+designation+whether they have reportees (mirrors the legacy system's approver-hierarchy + hardcoded-admin-allowlist patterns, cleaned up into 3 real roles). `roleAtLeast(role, minRole)` for hierarchy checks.
- `src/shared/access/role.store.js` — a small zustand store (`useSimulatedRole()`, `useHasRole(minRole)`, `useRoleActions().setSimulatedRole`) holding the **currently simulated** role, defaulting to `getEmployeeRole(CURRENT_USER)`. `CURRENT_USER`'s natural role computes to **Manager** (Finance dept, Tech Lead, has reportees) — not HR Admin — so admin-only actions are hidden by default until switched.
- A **"View As"** role switcher lives in Settings (`SettingsPage.jsx`) — since there's no login, this simulates a permission level for demo purposes without changing `CURRENT_USER`'s identity.
- Gating pattern used everywhere: `useHasRole("HR Admin")` gates admin-only buttons (Bulk Upload Documents, Bulk Import Employees, Add/Edit/Delete Employee, Policies Add/Edit + Policies document upload); `useHasRole("Manager")` gates the `isManager` flag used for every manager/reportee scope toggle (Attendance, Leave, Service Desk's Assigned Tickets tab) — **`isManager` must always be `MY_REPORTEES.length > 0 && useHasRole("Manager")`, never reportees-only**, or a role downgrade won't actually hide manager views (this was a real bug, since fixed everywhere).
- **Important scoping-state gotcha**: any manager/reportee scope toggle (`team`/`mine`) must make its underlying data filter re-check `isManager` too, not just the toggle's own `scope` state — otherwise downgrading role after "team" was already selected leaves stale reportee data visible even though the toggle UI disappears (real bug found and fixed in `AttendancePage.jsx`; the same pattern is correct in `LeavePage.jsx`).
- HR-SUPPORT's **Assigned Tickets** tab (only visible to Manager/HR Admin) uses the same idea at feature level: HR Admin sees every ticket, a plain Manager sees only tickets raised by `MY_REPORTEES`.
- **Employee Directory** takes this further: non-`HR Admin` users (Employee or Manager) see **only their own record** (`employees.filter(e => e.id === CURRENT_USER.id)`) in both the table and Organization Chart toggle context — not their team, just themselves. Only `HR Admin` sees the full directory + the Department/Location filters (the Active/Inactive status filter was removed outright, for every role).

## Validation
Every feature with a form has a `<name>.validators.js` exporting `validate<X>Form(form)` → errors object, called before the mutation fires, with `toast.error(Object.values(errors)[0])` on failure. This has been audited across the whole app — if you add a new form, follow this exact pattern (see `src/features/leave/leave.validators.js` as a clean reference). Don't duplicate validation logic inline in a component when a validators file already exists for that feature.

## Styling — Tailwind, and why the `.css` files are (mostly) empty
- Styling is Tailwind CSS v4 (`@tailwindcss/vite`), utility classes written directly in JSX `className` props.
- Every feature has a sibling `.css` file that's imported. Most are **intentionally empty** — don't add rules to them. A few (`attendance.css`, `leave.css`, `tax-declaration/taxdeclaration.css`) legitimately use `@reference "../../styles/tailwind.css";` + `@apply` for a handful of genuinely repeated Tailwind combos — this is the one exception to "don't write custom CSS," confirmed working (Tailwind v4's `@apply` needs that `@reference` line to resolve utilities inside a non-entry CSS file). Don't reintroduce broad hand-written CSS beyond this narrow pattern.
- `vite.config.js` has an explicit comment: *"The React and Tailwind plugins are both required for Make, even if Tailwind is not being actively used – do not remove them."* Don't remove `@tailwindcss/vite`, `tailwindcss`, or `tw-animate-css` from `vite.config.js`/`package.json`.
- Design tokens are real CSS custom properties in `src/styles/theme.css` (`:root` + `.dark`), exposed to Tailwind via a `@theme inline` block. Dark mode is a `.dark` class toggled high in the tree, not `prefers-color-scheme`.
- `src/styles/animations.css` defines global, non-Tailwind keyframe/utility classes: `animate-fade-in-up`, `animate-pop-in`, `animate-pulse-dot`, `hover-lift`, `card-shimmer`. Used fairly broadly now (page-entrance stagger, hover-lift on clickable cards, modal pop-in, sparing pulse-dot on ~2 live indicators) — respects `prefers-reduced-motion`. Don't add new keyframes; reuse these.
- Font: **IBM Plex Sans**. Brand color: brick red/dusty rose (`--primary:#9A2F33` light / `#E0797D` dark). Sidebar stays dark in both themes.

## Modal component
- `Modal` (`src/shared/components/Modal.jsx`) takes a `maxWidth` prop with a **literal Tailwind class string**, e.g. `maxWidth="max-w-lg"` (default `"max-w-md"`). Follow this pattern for any new modal. Some newer multi-step flows (Income Tax Declaration) render inline on the page instead of in a Modal — see that feature's notes below for when inline-vs-modal is the right call.

## CRUD / state pattern
- Pattern for a CRUD button: local `useState` for form fields → validate via the feature's validators file → `<Modal>` (`FormField` + `inputCls`) → on submit, call the query hook's mutation → `toast.success(...)` (errors are handled centrally, see Data layer section) → close modal. **Every action button must do something real** — never `toast.success(...)` alone.
- `downloadTextFile(fileName, content)` triggers a real Blob download — used for Documents "Download" and Policies "Download" (downloads the policy's uploaded `fileName` if HR Admin attached one, else a generated text summary).
- `ATTENDANCE` has no CRUD UI — fully read-only, derived straight from mock data.

## Key modules (behavioral notes)
- **Dashboard ("Home")** — personal/self-scoped only. No charts (explicitly removed per user request); animations (fade-in-up stagger, hover-lift, CountUp) are welcome. No Notifications card, no "Pending Leave Requests"/"Open Service Desk Tickets" grid, no Announcements — all removed. Layout below the profile/check-in row and quick-actions: a `lg:grid-cols-4` row where **My Attendance** takes `lg:col-span-3` (75%) and **My Leave Balance** + **Upcoming Holidays** stack in `lg:col-span-1` (25%) — nothing renders below this row (the full-width `HolidayCalendarGrid` that used to render last was removed per explicit request; the full Holiday Calendar still lives on the standalone `holidays` page, which "Upcoming Holidays" links to).
  - My Attendance: 6 stat cards — **Present, Early Exit, Late, Absent, On Duty, Forgot to Swipe** (computed in `DashboardPage.jsx`'s `monthStats`; "Early Exit" checks `checkOut < "18:00"` on Present days — always 0 with current mock data since checkout is fixed, but the logic is real; On Duty/Forgot to Swipe are counted via `LEAVE_TYPE_BY_EMP_DATE`, independent of the attendance record's own status, so they aren't mutually exclusive with Absent) — followed by the existing current-month attendance table (same Date/Check In/Check Out/Hours/Status data as before).
  - My Leave Balance: trimmed to just **Casual Leave** and **Privilege Leave** rows (`getLeaveBalance(...).filter(b => b.type === "Casual Leave" || b.type === "Privilege Leave")`) — the full 4-type balance is still shown elsewhere (Leave Management's balance modals).
- **Attendance** — a single flat nav item (no dropdown group, no sub-tabs — Regularization and Shift Change were removed entirely, along with Location Tracker). One view: Attendance Log with a My Reportees/My Attendance scope toggle for managers; when "My Reportees" is selected, an additional **reportee dropdown** (populated from `MY_REPORTEES`, "All Reportees" default) lets a manager narrow to one specific reportee. A **Table View / Calendar View** toggle (default Table) renders the same filtered records either as the existing `DataTable` or as a real month-grid calendar (`AttendanceCalendar` in `AttendancePage.jsx`, weekday columns × week rows, `StatusBadge`-pill legend, saturated per-status cell tint) showing check-in/check-out and a status derived from attendance + `HOLIDAY_DATA` + `LEAVE_TYPE_BY_EMP_DATE` (Holiday / actual leave type incl. "On Duty" / "Forgot to Swipe" for an unexplained absence / Present / Late). Calendar View is only enabled when exactly one employee is in scope (self, or a single selected reportee) — multi-employee scopes fall back to Table View.
- **Leave Management** — one section, **Leave Requests**. `LEAVE_TYPES` (in `shared/mock/constants.js`) is exactly `["Casual Leave", "Privilege Leave", "On Duty", "Forgot to Swipe"]` — **"Privilege Leave" is the renamed "Earned Leave"** (PL=EL is a standard Indian-HR synonym; don't reintroduce a separate "Earned Leave" type). `LEAVE_SESSIONS` is `["1st Session", "2nd Session", "Full Session"]`; `FORGOT_TO_SWIPE_REASONS` is `["Forgot to Bring ID", "Swipe Machine Down", "Others"]` (both also in `shared/mock/constants.js`).
  - Apply Leave has **two distinct flows** keyed on `leaveType`, both in `LeavePage.jsx`'s Apply Leave modal:
    - **Casual Leave / Privilege Leave / On Duty**: Start Date + End Date → every date in the range (`datesInRange()`) gets its own row with an independent **Session** dropdown (defaults to Full Session, tracked in a `dateSessions` state map keyed by date string) → a single free-text **Reason (optional)** textarea for the whole request, never validated as required. The leave record stores `dateSessions` (not a single `session`) plus `fromDate`/`toDate`/`days`.
    - **Forgot to Swipe**: a single date picker; once a date is chosen, a **Session** dropdown and a **Reason** dropdown (`FORGOT_TO_SWIPE_REASONS`, required) appear. The leave record stores a single `session` + `reason` + `fromDate === toDate`, `days: 1`.
    - The Leave Requests table's Session column and the View Details modal both handle both shapes (`r.session` for Forgot to Swipe, `Object.values(r.dateSessions)` summarized as "`N` dates" for the others; the View modal lists the full per-date breakdown when `dateSessions` has entries).
  - The **"My Reportees' Leave Balance"** button/modal is visible only when the "My Reportees" scope tab is active (manager) and shows every reportee's name + a compact per-type balance pill row (via `getLeaveBalance`); the plain **"Leave Balance"** button (visible on the "My Requests" tab, and always for non-managers) shows only `CURRENT_USER`'s own balance with **no** employee picker — which button renders is driven entirely by the `scope` state, not two independently-toggled buttons.
- **Documents** — two tabs: **Documents** (no table at all — a single Document Type + Date selection card; submitting looks up the matching document and downloads it with a real animated progress bar, reusing the same progress-simulation pattern as Employee Directory's Bulk Upload; errors via toast if no document matches) + **Income Tax Declaration** (ported from the legacy system's actual 5-step wizard UI, rendered inline on the page, not in a Modal; do not touch this tab's wizard flow when working on the Documents tab). Once submitted, `DeclarationSummary` (`features/tax-declaration/components/DeclarationSummary.jsx`) shows the green "Declaration Submitted" banner with the **Edit Declaration** button right-aligned directly below it (top of the summary, not at the bottom) — `hideEditButton` (used when this component is reused for a read-only review step) suppresses the button entirely rather than moving it.
- **Employee Directory** — a view toggle (**Employee Details** / **Organization Chart**, mirroring the Table/Calendar toggle pattern used in Attendance) switches between the employee table and `OrgPage`'s org-chart tree (relocated here from Services — see below). In Employee Details: `HR Admin` sees the full directory + Department/Location filters + Add/Edit/Delete/Bulk actions; Manager and Employee both see **only their own record** (no filters, no admin buttons — see RBAC section). "View" opens the employee-details modal in place, no redirect (a legacy `setModule("documents")` side effect under the modal was removed).
- **HR-SUPPORT** (renamed from "Service Desk", module id still `servicedesk`, on-page heading reads "HR-Support") — two tabs: **My Tickets** (tickets where `employeeId === CURRENT_USER.id`) and **Assigned Tickets** (Manager/HR Admin only, gated by `isManager = MY_REPORTEES.length > 0 && useHasRole("Manager")` OR `isHrAdmin`). `TICKET_CATS` (`servicedesk.mock.js`) is the fixed 18-item list ported from LEENA AI / the legacy HR Helpdesk system: `Admin, Login Issue, Medical Insurance, Payroll/Salary, Provident Fund/PF, Relocation, Access Request/ID Card, Visitor Lunch, Stationery, Repair and Maintenance, Onboarding, Internal Job Posting (IJP), Learning & Development, Statutory Compliance, Performance Management, Prestige Stars, Talent Management, Others`. **No Priority field anywhere** in this feature (form, filters, table column, ticket data) — removed per explicit request.
  - Raise Ticket branches on `category` into two flows, `BESPOKE_CATS` (`servicedesk.mock.js`) marking which is which:
    - **4 bespoke categories** (`Access Request/ID Card`, `Visitor Lunch`, `Stationery`, `Repair and Maintenance`) get a dedicated multi-field form ported field-for-field from the legacy "Call Register" screens (Lunch: Lunch Request/From Date/To Date/Count/Description; Repair and Maintenance: Repair Items/Count/Floor/Description; Access Request/ID Card: Blood Group/Upload Image/Description + two red compliance notes about jpg/512KB and the Rs. 300 salary deduction; Stationery: Stationary Category→Item (dependent)/Item Description/Quantity/Unit/Department (read-only, `CURRENT_USER.department`)/Floor/Description + a red note about Designation/Mobile Number changes). None of these 4 have a generic Attach Files field except ID Card's own "Upload Image" — match the screenshots, don't add one.
    - **The other 14 categories** get a generic Category → **Sub Category** (dependent dropdown, `SUB_CATEGORIES_BY_CATEGORY[category]` in `servicedesk.mock.js` — the exact cascading map supplied by the user, not invented) → Description → optional **Attach File** form, ported from the LEENA AI ticketing-form screen recording. `NO_SUBCATEGORY_CATS` (`Others`, `Talent Management`) have no sub-category breakdown — the Sub Category field is hidden entirely for these two, form goes straight from Category to Description. There is no free-text Subject field in any flow — `ticket.subject` is auto-derived (`"${category} - ${subCategory}"` for generic, plain `category` for the two no-subcategory ones, `"${category} Request"` for bespoke) purely for table/detail display.
  - Category and Sub Category fields (both in the Raise Ticket modal and the table's Category filter bar) use the shared `SearchableSelect` component rather than a plain `<select>`, since the option lists are long. Raise Ticket's Category field starts unselected (`emptyNewTicket.category = ""`) with a `placeholder="Please select Category"` — it must never default to the first `TICKET_CATS` entry, same "blank until chosen" treatment as Sub Category.
  - `ServiceDeskPage.jsx`'s `CATEGORY_FIELD_LABELS` map renders whichever `ticket.categoryDetails` (bespoke) or `ticket.subCategory`/`ticket.description`/`ticket.attachmentName` (generic) fields are present in the Ticket Details modal.
  - The Status/Category filter bar wraps both fields in equal `min-w-[160px]` divs with matching `py-2` sizing so the plain `<select>` (Status) and `SearchableSelect` (Category) render at the same height/width. The filter bar div itself carries `relative z-10` — needed because `animate-fade-in-up`'s keyframe ends on `transform: translateY(0)` (not `none`), which with `animation-fill-mode: both` permanently creates a new stacking context on every element using that class; without an explicit higher z-index the filter bar (and its SearchableSelect popup) would paint *underneath* the later `animate-fade-in-up` DataTable sibling. Any future absolutely-positioned popup (tooltip, dropdown) placed before a `DataTable` on the same page needs the same treatment.
- **Services is no longer a page** — `ServicesPage.jsx`/`services.css` were deleted; `services` is now a Sidebar dropdown *group* (`nav.js`'s `children`) containing **Learning Portal** (id `learning`), **Conference Room** (id `conference`), **Doctor Appointment** (id `doctor`) as direct top-level routes in `App.jsx` — each renders its own page component straight from the sidebar/bottom-nav, there's no intermediate tabs UI or composition page anymore. Organization Chart, Policies, and Holiday Calendar used to live here as tabs/children too — all three were relocated to top-level nav items (Org Chart into Employee Directory's toggle, Policies into its own `policies` nav item, Holiday Calendar to its own `holidays` nav item positioned right after Policies — see Navigation structure section).
  - **Learning Portal** — trimmed to a single clickable **URL** column (`course.url` in `learning.mock.js`); category/instructor/progress/employee/due-date/status columns and the stat cards were removed.
  - **Conference Room Booking** — ported from a legacy ASP.NET "Call Register"-style booking page, ties booking to a specific Location → Room (`CONFERENCE_LOCATIONS` / `CONFERENCE_ROOMS_BY_LOCATION` in `conference.mock.js` — the exact location list supplied by the user; room-to-location mapping is *inferred* from the room names themselves since only two flat lists were given, not a mapping — rooms explicitly naming a Hosur unit are scoped there, everything else is a floor of the one multi-floor building and scoped to Bangalore Corporate Office; Banglore Factory has no rooms in the supplied data). Bangalore Corporate Office's room list starts at `"First Floor-PUNARVASU-(Meeting Room)"` — the 3 "Conference Hall-..." entries the legacy list originally led with were removed per explicit request. Location and Room both use the shared `SearchableSelect` component (not a plain `<select>`), matching the font/sizing pattern used elsewhere. A **Calendar / Table** view toggle (default Calendar) covers 8 AM–8 PM in 15-minute slots (`TIME_SLOTS`, minutes-since-midnight is the internal unit, not the legacy's "H.MM" decimal quirk):
    - **Calendar** is a Teams/Outlook-style continuous time grid, not a dense per-slot table: a CSS grid (`56px` time gutter + `repeat(7, minmax(120px,1fr))` day columns) sized at `PX_PER_MIN = 1` (`GRID_HEIGHT = (SLOT_END_MIN - SLOT_START_MIN) * PX_PER_MIN`, i.e. 1px per minute, ~720px tall, scrolling inside a `maxHeight: 560` wrapper), with hour gridlines solid and half-hour gridlines dashed (`HOUR_MARKS`/`HALF_HOUR_MARKS`). Bookings render as one absolutely-positioned block per booking (`top`/`height` computed straight from `start`/`end` in minutes) spanning its real duration — not one row per 15-minute slot — showing time + employee name directly on the block; a red current-time line + dot (`showNowLine`/`nowTop`) crosses today's column when now falls inside business hours, and the already-elapsed portion of today's column is shaded. Clicking anywhere in a day column (`handleColumnClick`) maps the click's pixel offset back to the nearest 15-minute slot (`Math.floor(offsetY / PX_PER_MIN / SLOT_INCREMENT_MIN) * SLOT_INCREMENT_MIN`) and opens the booking modal in add mode, or edit mode if that slot falls inside your own booking — there's no more one-`<td>`-per-slot click target. Hovering a block shows the same `group-hover` tooltip card as before (date/time/employee/contact/email/purpose, plus inline Edit/Cancel when it's yours) — the block itself must NOT carry `overflow-hidden` (only its inner `<p>` text uses `truncate`), since `overflow-hidden` on the block clips its own absolutely-positioned tooltip child; the tooltip still flips `bottom-full`/`top-full` based on the block's pixel `top` so it doesn't get clipped by the scroll container. The time gutter's hour labels are vertically centered on their gridline (`-translate-y-1/2`) *except* the first (`08:00`, no translate — its natural top-aligned position) and last (`20:00`, `-translate-y-full`) — without that clamp the centering transform pushes those two edge labels half-off the grid, where the first gets visually clipped by the sticky header row above and the last by the grid's own bottom edge.
    - **Table** lists the same 48 slots for one selected date, **Action is the first column** (Book button on available rows; Edit+Cancel icon buttons on your own booked rows; "View only"/"Past" text otherwise) per explicit request — don't reorder this.
    - Add and Edit share one `Modal` (title "Book Room" vs "My Booking"): Date + Start/End Time (selects, not free text), Purpose (50-char limit + counter), Attendees, Breakfast/Tea/Lunch (`MEAL_OPTIONS`: Not Required/Required — the legacy's CRPO-only meal restriction was dropped since no supplied location matches "CRPO"). Edit shows Update + Cancel Booking (destructive) + Close; Add shows Book + Close. Editing changes the whole booking's date/time (not the legacy's per-15-min-slot splitting of one record — simplified on purpose, still fully functional).
    - `conference.service.js` is the one exception to this app's usual "service always resolves the static seed array" mock pattern (see Data layer section) — its `addBooking`/`updateBooking`/`cancelBooking` actually mutate the shared `BOOKINGS` array in place, so a booking survives the post-mutation `invalidateQueries` refetch instead of reverting a moment later. If the app-wide persistence bug ever gets fixed centrally, this file can be simplified back to the standard pattern.
  - **Doctor Appointment** (`src/features/doctor/`, id `doctor`) — built from the "Booking Doctor Appointment" legacy screen recording; replicates the *business rules* (a single shared doctor resource, 15-minute slots, one booking per slot, auto-computed end time), not the legacy's literal yellow-box form styling — rendered with this app's normal `FormField`/`inputCls` design language instead. The clinic holds session every **Friday, 2:30 PM – 5:30 PM** (`CLINIC_WEEKDAY`/`SLOT_START_MIN`/`SLOT_END_MIN` in `doctor.mock.js`); **Booking Date** only offers the next 4 upcoming Fridays (`upcomingClinicDates()`); **Employee Number/Age/Department/Gender** are read-only, auto-filled from `CURRENT_USER` (Age and Gender are deterministically derived from `employeeId` since the shared Employee model has neither field yet — same self-contained-pseudo-field pattern as `settings/uniform.mock.js`, don't invent a third variant elsewhere); **Available Slots** excludes any slot already booked by *any* employee for the selected date (shared-resource conflict check, same idea as Conference Room); **End Time** is read-only, auto = start + 15 min. Below the booking card, a **My Appointments** `DataTable` lists only the current user's own bookings (Date/Start/End/Status) with a Cancel action on upcoming `Scheduled` rows — `StatusBadge` gained `Scheduled` (blue) and `Cancelled` (gray) color entries for this. Like Conference Room, `doctor.service.js` mutates its `APPOINTMENTS` array in place (the same deliberate exception to the app's read-only-mock convention) so a booking/cancel survives the post-mutation refetch.
- **Policies** — its own top-level nav item (`src/app/nav.js` id `policies`, routed directly in `App.jsx`), no longer nested under Services. Two orthogonal toggles at the top: a **Policy Type** toggle (`POLICY_TYPES` in `policies.mock.js`, in this exact tab order: `Company Policies` / `HR Policies` / `IT Policies`) and a **Folder View / Table View** toggle (default Folder View) that apply independently to whichever policy type is selected.
  - **Folder View** — a collapsible "Folders" grid of topic-folder tiles (folder icon in a circle, name, live `N Article(s)` count) per `POLICY_FOLDERS_BY_TYPE[policyType]` (e.g. HR's 18 folders: Attendance, Holiday, Incentives, Insurance, Internal Job Mobility, Learning Management, Leave, Others, Payroll, Payslip, Performance, PF, POSH, Relocation, Salary, Separation Management, Travel, Vehicle — ported from the legacy HR portal's folder screen; IT Policies and Company Policies have their own smaller folder sets). Clicking a folder drills into a `DataTable` of that folder's policies (columns: Policy Name/Folder/Department/Version/Effective Date/Status) with a "Back to Folders" breadcrumb; folder counts are computed live off the query cache, not hardcoded, so Add/Edit Policy updates them immediately. **Only HR Policies ships with seed documents** (`SEEDED_POLICY_TYPES`, exported from `policies.mock.js`) — for IT Policies and Company Policies, Folder View shows a plain empty state (`Inbox` icon + "No policies added yet") instead of folder tiles entirely (not tiles-with-0-Articles) until a real policy is added via Add Policy; `POLICY_FOLDERS_BY_TYPE` still defines their folder sets for when that happens.
  - **Table View** — a flat `DataTable` (columns: Policy Name/Policy Category/Effective Date) filtered to the selected policy type — no Folder/Department dropdown filters on this view. CSV export is hidden on both Policies tables (`DataTable`'s `hideExport` prop; Print is gone globally, see below).
  - Row actions (both views): Download (all users) + Edit (HR Admin only, pencil icon) — no per-row Delete.
  - "Download" does a real `downloadTextFile` (uses the policy's uploaded `fileName` if present, else a generated text summary); Add/Edit Policy modal (Add button HR Admin only; Edit pencil is also HR Admin only) has **Policy Name** + (HR Admin only) **Policy Document Upload** (`.pdf`/`.doc`/`.docx`, sets `form.fileName`) + **Policy Type** + **Policy Category** (a `SearchableSelect` scoped to `Object.keys(POLICY_FOLDERS_BY_TYPE[form.policyType])` — strictly one of that type's predefined folders, no longer a free-text `<datalist>` input, so a new folder must be added to `POLICY_FOLDERS_BY_TYPE` before it can be picked here) + **Effective Date**. `emptyForm.department` defaults to `"All"` (not empty) though there's no Department field in the form itself.
- **Holiday Calendar** — its own top-level nav item (id `holidays`), positioned directly below Policies in `nav.js` (moved out of the Services dropdown group — see Services notes above). Header controls (a `Location` `SearchableSelect` + a `Year` pill toggle) sit above the **Table / Calendar** view toggle and apply to both views identically, replacing the old free-text search box and Type dropdown (both removed entirely — `DataTable` gained a `hideSearch` prop for this, mirroring the existing `hideExport`). `holidays.mock.js` now seeds **two years** of holiday data (current + previous, derived from `HOLIDAY_TEMPLATES`, `day` computed from `date` rather than hand-maintained) so the year toggle has real data to compare — `latestYear`/`latestYear - 1` are derived from the data itself (`Math.max` over the dataset's years), not hardcoded or read from the system clock, so the toggle stays correct as more years are added. The Location dropdown filters on `LOCS` (`shared/mock/constants.js`) against each holiday's `location` field (`"All"` always matches; multi-location holidays store a comma-separated string). `HolidayCalendarGrid` (`features/holidays/components/HolidayCalendarGrid.jsx`) was converted from internally-managed month state to a controlled component (`month`/`onMonthChange` props, falling back to internal state if omitted) so the parent page can jump the visible month when the year toggle changes — it is still the one shared calendar-grid component, don't duplicate its markup.
- **Reports module has been removed entirely** — no nav item, no route, no `src/features/reports/` folder. Don't recreate a cross-feature reporting page without being asked.
- **Location Tracker module has been removed entirely** — no nav item, no route, no `src/features/location/` folder.
- **Settings** — Profile/Appearance/Notifications (original) + **View As** role switcher + **Uniform Size Declaration** (gender-gated via a self-contained pseudo-flag since `Employee` has no real gender field — see `uniform.mock.js` for the note on promoting this to a shared field later) + **Employee Feedback** (location-scoped recipient list, append-only).
- **TopNav** — no message/chat icon (removed), and the global search bar has also been removed entirely (no search input, no `globalSearch.store.js` — deleted, was employees/tickets/documents dropdown results). Just theme toggle, notifications bell, user menu, and "My Profile" modal + breadcrumb via `findNavLabel()`. The notification and user-menu dropdowns close on click-outside (`notifRef`/`userRef` + a `mousedown` document listener, same click-outside pattern as `SearchableSelect`) — don't reintroduce a dropdown here without wiring the same click-outside handling.
- Shared `DataTable` toolbar: Search/CSV only (no copy-to-clipboard, no Print — the Print button/icon was removed from `DataTable` entirely) — pass `hideExport` to suppress CSV for a specific table (used by Policies) and `hideSearch` to suppress the search box (used by Holiday Calendar, which replaced free-text search with its own Location/Year filters).
- `SearchableSelect` (`src/shared/components/SearchableSelect.jsx`) — button-triggered dropdown with a search input + scrollable (`max-h-52 overflow-y-auto`) option list, click-outside-to-close; accepts `options` as `string[]` or `{value,label}[]`. Used by HR-SUPPORT's Category/Sub Category (Raise Ticket modal) and its table Category filter, Policies' Add/Edit Policy Category, and Holiday Calendar's Location filter. Reach for this instead of a plain `<select>` whenever a dropdown's option list is long enough to warrant search + scroll. Its trigger button and option-list buttons both carry an explicit `font-normal` — the app's global base style bolds every `<button>` (`font-weight: var(--font-weight-medium)` in `theme.css`), so without that override a `SearchableSelect` reads visibly bolder than a plain `<select>` sitting next to it (only the selected option's row keeps `font-medium` for emphasis).
- Shared `Btn` (`src/shared/components/Btn.jsx`) supports a `disabled` prop (forwarded to the underlying `<button>` with `disabled:opacity-50 disabled:cursor-not-allowed` styling) — it silently no-op'd before, so always check this component actually supports a prop before assuming it does.
- `StatusBadge` (`src/shared/components/StatusBadge.jsx`) has color entries for `"On Duty"` and `"Forgot to Swipe"` (added for the Attendance calendar) and `"Scheduled"`/`"Cancelled"` (added for Doctor Appointment), alongside the pre-existing status set.
- `HolidayCalendarGrid` (`src/features/holidays/components/HolidayCalendarGrid.jsx`) is the extracted, reusable month-grid calendar (prev/next month nav, holiday/today/weekend styling+legend) — used by both `HolidaysPage.jsx`'s Calendar view and the Dashboard's full-width Holiday Calendar. Don't duplicate this markup; extend the shared component if the calendar needs new behavior.

## Navigation structure
`src/app/nav.js` supports **grouped/collapsible dropdown nav items** (an item with `children: [...]` renders as an expandable group in `Sidebar.jsx`). `NAV_ITEMS` is mostly flat — `Home` (id `dashboard`), `Employee Directory`, `Attendance`, `Leave Management`, `Documents`, `HR-SUPPORT` (id `servicedesk`), `Policies`, `Holiday Calendar` (id `holidays`, placed directly after Policies), `Settings` — except `Services`, which is the one dropdown group: it expands to `Learning Portal` (id `learning`), `Conference Room` (id `conference`), `Doctor Appointment` (id `doctor`), each a real top-level route in `App.jsx` (there's no `services` page itself — clicking the group header only expands/collapses it, or in collapsed-sidebar mode jumps straight to the first child). Holiday Calendar used to be a Services child too but was pulled out to a flat top-level item — moving a nav item between flat and grouped only ever touches `nav.js`, never the module id or its `App.jsx` route (same rule as the Dashboard→Home / Service Desk→HR-SUPPORT label renames — see Preferences below). Attendance no longer groups with Location Tracker (removed) — nothing currently needs `minRole` at the nav-item level. `FLAT_NAV_ITEMS`, `findNavLabel()`, `findParentGroupId()` are exported for flat lookups (breadcrumb, active-group-highlighting) and keep working unchanged whether or not any item currently has `children`. An item can still carry `minRole` for role-gating (see RBAC section) — `Sidebar.jsx` filters on this and falls back to Dashboard if the active module becomes hidden after a role downgrade.

## Preferences (feedback memory candidates)
- Department filters/columns are unwanted anywhere in the app.
- Dashboard must stay personal/self-scoped — no org-wide tables/charts.
- Manager/reportee scoping is a running theme — always gate the underlying `isManager` flag on BOTH reportee-count AND role (`useHasRole("Manager")`), and always make scope-toggle-filtered data re-check `isManager`, not just the toggle state (see RBAC gotcha above).
- **Every action button must do something real** — never `toast.success(...)` alone; wire it to a real mutation.
- Large single-line/minified reference files should be mined with grep/agents, never `Read` directly.
- Don't convert Tailwind to plain CSS (tried once, explicitly reverted) — the narrow `@reference`+`@apply` exception above is the one allowed departure from pure inline utilities.
- When porting legacy-system logic: replicate the *business rules*, not the legacy UI — the one explicit exception so far is Income Tax Declaration's step-wizard structure, requested by name.
- This project has been extended heavily by parallel background agents in past sessions — when doing another large multi-file rollout, group work by feature folder, explicitly exclude cross-cutting consumer files (TopNav/DashboardPage/EmployeesPage) from parallel agents' scope, and do those centrally yourself afterward to avoid edit conflicts.
- Modules can be removed wholesale (Reports, Location Tracker, Attendance's Regularization/Shift Change tabs, Leave's Comp-Off/On-Duty/WFH tabs all were) — when doing so, delete the feature's files outright (mock/service/validators/hooks/component) rather than leaving them unreferenced, and grep the rest of the app for the deleted feature's query hooks/mock exports before considering the removal done (Dashboard, TopNav, and any former Reports-style aggregator are the usual places a stale import hides).
- Renaming a nav item's *label* (Dashboard→Home, Service Desk→HR-SUPPORT) should never touch the module `id` — routing (`App.jsx`), RBAC gating, and global search all key off the id, so only `nav.js`'s `label` and any on-page `<h2>` heading text need to change.
- A category/type list can be replaced wholesale (leave types down to 4, ticket categories to the LEENA-AI/HR-Helpdesk 18-item list) without needing to preserve prior per-category bespoke UI — check whether the new list's items still semantically match any existing extra-fields/sub-forms before carrying them forward; when they don't (as with the ticket categories), delete the old bespoke system's files outright rather than trying to remap it.
- Components worth extracting for reuse rather than duplicating: `HolidayCalendarGrid` (shared by HolidaysPage and Dashboard) is the template — if the same nontrivial markup+state is about to be copy-pasted into a second page, pull it into a `components/` file in the owning feature instead.

## Build
```
npm install
npx vite build   # or: npm run build
npm run dev      # local dev server (kill port 5173 first if reusing: lsof -ti:5173 -sTCP:LISTEN | xargs -r kill)
```

## Key dependencies added this project (beyond the original scaffold)
`zustand` (local/UI state), `@tanstack/react-query` + `axios` (server-state + future real API layer).
